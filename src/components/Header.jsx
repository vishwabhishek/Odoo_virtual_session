import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <div className="header-title">Skill Swap Platform</div>
      <button className="login-btn">Login</button>
    </header>
  );
};

export default Header; 